# Copy this file to `.env.mem0` and fill in the required secrets for the Mem0 server.
# Any variables included here will be injected into the `mem0-server` container.

# Core LLM provider keys (choose the one you intend Mem0 to use)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# Toggle Mem0 features
ENABLE_GRAPH=true
ENABLE_VECTOR_STORE=true

# Qdrant + Neo4j credentials (the defaults match docker-compose.yml)
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=mem0-graph
